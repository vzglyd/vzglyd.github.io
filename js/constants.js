/**
 * constants.js — Shared runtime constants.
 *
 * These values must stay in sync with the authoritative definitions in
 * vzglyd_kernel (VRX-64-kernel/src/manifest/mod.rs).
 */

export const MIN_DISPLAY_DURATION_SECONDS = 1;
export const MAX_DISPLAY_DURATION_SECONDS = 300;
